required packages:
------------------

numpy (standard for Python > 2.7, other standard libraries will not be mentioned)

non-standard libraries:
scipy
psutil
platform? (not sure if this is in the standard library or not)